import React from 'react';
import ReactDOM from 'react-dom';
import './app/index.css'; // Adjust path if needed
import App from './app/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);